add_namespace = YSS

country_event = {#接收器
    id = YSS.1
    hidden = yes    

    trigger = {
        YSS = {
	        exists = yes
        }
    }
    fire_only_once = yes

	immediate = {
        YSS = {
            set_country_flag = jindu3_has_more
        }
    }
}