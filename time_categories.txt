time_categories = {

	MYC_gift = {

		cost = 100

		complete_effect = { add_political_power = 100 }
		cancel_effect = { add_political_power = 100 }
	}

	give_the_first = { complete_effect = { set_country_flag = the_first } }

	delete_the_first = { complete_effect = { clr_country_flag = the_first } }

	give_the_second = { complete_effect = { set_country_flag = the_second } }

	delete_the_second = { complete_effect = { clr_country_flag = the_second } }

		give_the_third = { complete_effect = { set_country_flag = the_third } }

	delete_the_third = { complete_effect = { clr_country_flag = the_third } }

	time_mission = {
		days_mission_timeout = 100

		available = { has_country_flag = the_first }

		cancel_trigger = { has_country_flag = the_second }

		activation = { has_country_flag = the_third }

		timeout_effect = { add_political_power = 100 }
	}

	add_time = { 
		complete_effect = { 
			add_days_mission_timeout = {
				mission = time_mission
				days = 20
			} 
		}
	}

	cut_time = { 
		complete_effect = { 
			add_days_mission_timeout = {
				mission = time_mission
				days = -30
			} 
		}
	}

	build_a_road = {
		complete_effect = {#5117 13370 12775 7219 8048 1950
			build_railway = {	#upgrades existing railway
						level = 1
						fallback = yes
						path = { 5117 13370 12775 7219 8048 1950 }
						start_province = 5117	
						target_province = 1950
					}
		}
	}
}