MYC_decisions = {
	
	
	ZSB_cost_gun_0 = {
		custom_cost_text = decision_cost_ie_1000

		custom_cost_trigger = { NOT = { has_equipment = { infantry_equipment < 500 } } }
	}


	ZIM_open_mission = {
		icon = generic_construction

		complete_effect = {
			activate_mission = ZIM_region_mission
		}
	}

	ZIM_region_mission = {
		icon = generic_construction
		days_mission_timeout = 10

		activation = {
			always = no
		}

		available = {
			always = no
		}

		highlight_states = {
			highlight_state_targets = {
				state = var:chosen_region
			}
		}

		timeout_effect = {
			add_political_power = 50
		}
	}

	get_political = {
		complete_effect = {
			add_political_power = 100
		}
	}

	launch_situation = {
		remove_effect = {
			activate_mission = terrible_situation
		}
	}

	terrible_situation_0 = {
    	selectable_mission = yes
    	cancel_if_not_visible = yes
    
	    # 30天一个周期
    	days_mission_timeout = 15
    
    	# 可用性：需要触发标志（由国策/事件开启）
    	available = { has_country_flag = bandit_crisis_active }
    
    	# 倒计时结束执行
    	timeout_effect = {
	        # 随机选择一个拥有的省份
        	random_owned_state = {
    	        # 可选：添加限制，比如只选有基建的地区
	            limit = {
            	    has_building = { type = infrastructure level > 0 }
        	    }
            
    	        # 破坏基建
	            damage_building = {
                	type = infrastructure
        	        damage = 1.5
            	    repair_speed_modifier = -0.3
    	        }
            
	            # 可选：添加一个事件通知玩家（更真实的代入感）
            	# country_event = { id = bandit_crisis_report.1 }
        	}
		
	        # 循环：30天后再次执行
        activate_mission = bandit_crisis_loop
    	}
	}

	stop_situation = {
		custom_cost_trigger = {
			NOT = { has_equipment = { infantry_equipment < 2000 } }
		}

		days_remove = 30

		remove_effect = {
			set_country_flag = stop_damage
		}
	}
}
